# ADR-007: 评测目标采用独立版本并支持基线对比

**Status:** accepted

AI 评测系统以一个固定的系统基线作为初始参照，同时为每个 Agent、Workflow 和 Pipeline 建立独立的不可变评测目标版本。评测目标版本关联代码、Prompt、Skill、工具协议、模型配置和评测依赖的完整快照；默认将最新版本与其 parent 版本进行 Benchmark、自动评分和人工对比评测。这样可以隔离不同目标之间的变更，保证评测结果可追溯，并支持管理员判断一次修改是否真正改善了目标行为。
