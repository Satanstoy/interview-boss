# ADR-006: MCP 面试读写权限分离

**Status:** accepted

MCP 面试能力按 `mcp:read` 和 `mcp:write` 分离：读取候选人画像、面试记录、报告、状态和题库只需要读权限；创建导入任务、上传分块、推进回合、重试分析、续接和结束面试需要写权限。写权限不足时请求必须在创建任何半成品之前拒绝。

所有权限判断基于当前认证 Token 绑定的 IB 用户，不接受客户端指定用户身份。
