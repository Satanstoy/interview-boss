# 面试 Agent 质量问题根因分析

## 关键发现：E2E 脚本与真实后端的差异

**`scripts/test_full_interview.py` 是直接调 LLM API（绕过后端 pipeline），不是通过 InterviewBoss 后端跑的。** 这意味着：

- Thinking 泄露、总结质量、反问处理等问题**可能只存在于直调场景**，后端 pipeline 有额外保护
- 但也说明 **prompt 本身存在薄弱环节**，因为 prompt 是直调和后端共用的

---

## 问题 1：总结质量低（第三轮 0/10）

### 根因：E2E 脚本的硬编码 15 轮限制 + Prompt 缺乏主动收尾激励

**E2E 脚本侧** (`scripts/test_full_interview.py`):
- `MAX_INTERVIEW_TURNS = 15`（第 25 行）——每轮面试最多 15 个 user 消息
- 达到 15 轮后直接 `break`，**面试官没有任何收尾/总结**
- 候选人模拟器也**没有说过"结束面试"**这类触发词

**后端 pipeline 侧**:
- `_determine_interview_phase` 在 <=32 条消息时提示"面试进行中"
- 只有 >44 条才强制收尾
- Prompt 的收尾指令（prompts.py:50-56）是"当你认为已经充分考察了..."——这是一个**软指令**，LLM 很容易忽略
- Prompt 说"候选人问完后简单回应即可结束，不需要总结评价"——**明确告诉面试官不要总结**，这与评估标准矛盾

**结论**：
1. E2E 脚本的 15 轮限制太短，面试还没进入收尾阶段就被截断
2. Prompt 的收尾指令过于被动（"当你认为已经充分考察了"），缺乏**量化触发条件**
3. Prompt 明确说"不需要总结评价"，导致总结质量天然为 0

---

## 问题 2：反问处理差（第三轮 0/10）

### 根因：Prompt 中反问处理指令被淹没 + 面试风格约束过于严格

**Prompt 结构问题**:
- "候选人反问处理"只占 prompt 的 6 行（prompts.py:78-84）
- 而"面试风格"中"不要鼓励候选人"、"不要给改进建议"、"不要总结亮点"占了 7 行且标为"严格遵守"
- **"不要鼓励"和"要回应反问"在语义上冲突**——LLM 倾向于执行更强烈的约束（不鼓励），忽略了较弱的约束（回应反问）

**面试官角色设定**:
- 开头就说"你不会鼓励候选人，不会给改进建议，只会不断追问"——这个 persona 过于单一
- LLM 在这个 persona 下很难切换到"友好回应反问"的模式

**结论**：
1. 反问处理指令的优先级/权重不够，被"不要鼓励"等强约束压制
2. 面试官 persona 过于"冷峻"，缺乏灵活性

---

## 问题 3：Thinking 泄露（面试官自言自语）

### 根因：E2E 脚本直调 LLM，绕过了 pipeline 的 thinking/content 分离

**E2E 脚本** (`scripts/test_full_interview.py`):
- 用 `requests.post(f"{BASE_URL}/chat/completions")` 直接调 LLM
- 只取 `choices[0].message.content`，**没有 thinking/content 分离逻辑**
- mimo-v2.5-pro 模型可能在 content 中输出推理过程（"这个追问很自然，而且候选人提到了..."）

**后端 pipeline 侧**:
- `llm.py` 的 `stream_llm_messages` 有完善的 thinking 分离（检测 ThinkingBlock、reasoning_content）
- `pipeline.py` 的 `_stream_final_answer` 只把 `event_type == "content"` 的 chunks 拼接成最终文本
- **后端不会出现这个问题**

**结论**：
1. 这是 E2E 脚本的 bug，不是后端/prompt 的问题
2. 如果要修复 E2E 脚本，需要在 `call_llm()` 中过滤掉 thinking 内容

---

## 总结：三个问题的分类

| 问题 | 类型 | 影响范围 | 修复方向 |
|------|------|---------|---------|
| 总结质量低 | **Prompt + E2E 脚本** | 后端也受影响（prompt 说"不需要总结"） | 1) E2E 脚本增加主动收尾逻辑 2) Prompt 收尾指令改为"必须生成个性化总结" |
| 反问处理差 | **Prompt** | 后端也受影响 | 提升反问处理指令优先级，加入"必须回应"的强约束 |
| Thinking 泄露 | **E2E 脚本 bug** | 仅 E2E 脚本 | 修复 E2E 脚本的 call_llm()，过滤 thinking 内容 |

## 修复建议

### 1. Prompt 修复（影响后端）
- **收尾指令**：把"不需要总结评价"改为"必须生成个性化总结，包含整体评价、技术亮点、改进建议"
- **收尾触发**：增加量化条件，如"覆盖项目+八股+算法各 1 轮后，主动进入收尾"
- **反问处理**：提升优先级，改为"候选人反问时必须回应，这是面试的基本礼貌"

### 2. E2E 脚本修复
- `MAX_INTERVIEW_TURNS` 从 15 提高到 20-25
- 候选人模拟器增加主动结束触发（如第 12 轮后说"可以结束了"）
- `call_llm()` 增加 thinking 内容过滤

### 3. 后端 pipeline 修复（可选）
- `_determine_interview_phase` 的收尾阈值从 44 调低到 30-35
- 增加"已覆盖知识点"计数器，当覆盖度足够时提前触发收尾
