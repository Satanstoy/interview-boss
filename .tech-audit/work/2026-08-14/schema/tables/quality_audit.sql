CREATE TABLE quality_audit (id INTEGER PRIMARY KEY AUTOINCREMENT,audited_at TEXT NOT NULL,sample_size INTEGER NOT NULL,total_variants INTEGER NOT NULL,inconsistent_count INTEGER NOT NULL,duplicate_count INTEGER NOT NULL,coverage_count INTEGER NOT NULL,inconsistent_rate REAL NOT NULL,duplicate_rate REAL NOT NULL,coverage_rate REAL NOT NULL,report_path TEXT,triggered_cleanup INTEGER DEFAULT 0);
